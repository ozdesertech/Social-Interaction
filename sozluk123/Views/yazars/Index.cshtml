@model IEnumerable<sozluk123.Models.yazar>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.yazar_ismi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.yazar_yorum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.kayit_tarih)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.active)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.yazar_ismi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.yazar_yorum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.kayit_tarih)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.active)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
